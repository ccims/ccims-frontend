mutation CreateIssue($input: CreateIssueInput!) {
  createissue(input: {
    title: "Bug in API"
    body: "# Problem\n- Some __Bug__"
    componentIDs: ["COMPONENT_ID"]
    category: BUG
    locations: ["LOCATION_ID"]
  })
}
